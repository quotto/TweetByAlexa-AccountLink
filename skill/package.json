{
  "name": "tweetbyalexa",
  "version": "1.0.0",
  "private": true,
  "description": "TweetByAlexa",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "@powershell -NoProfile -ExecutionPolicy Unrestricted -Command ./deploy/deploy.ps1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "alexa-sdk": "^1.0.24",
    "request": "^2.83.0"
  }
}
